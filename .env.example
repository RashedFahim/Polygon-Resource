# The frontend does not require Vite environment variables.
#
# For the PHP endpoint, copy contact-config.example.php to contact-config.php
# outside public_html and fill in the server-side SMTP values there.
